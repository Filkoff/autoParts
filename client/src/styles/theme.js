import { createTheme } from '@material-ui/core';

export const theme = createTheme({
  typography: {
    fontSize: 16,
  },
  palette: {
    primary: {
      main: '#ffbf33',
    },
    secondary: {
      main: '#000000',
    },
    gray: {
      main: 'a1a1a1',
    },
  },
});
